/**
 * Props for HeroType.
 *
 * @param title - Visible/accessible heading text rendered in the SVG and a visually hidden h1.
 * @param className - Optional class merged onto the root element.
 *
 * @example
 * const props: HeroTypeProps = { className: 'my-class' }
 */
export interface HeroTypeProps {
  className?: string;
}
