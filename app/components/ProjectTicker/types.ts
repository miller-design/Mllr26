/**
 * Props for ProjectTicker.
 *
 * @example
 * const props: ProjectTickerProps = { className: 'my-class' }
 */
export interface ProjectTickerProps {
  /** Optional class for the root element. */
  className?: string
}
