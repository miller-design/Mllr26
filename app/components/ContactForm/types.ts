/**
 * Props for ContactForm.
 *
 * @example
 * const props: ContactFormProps = { className: 'my-class' }
 */
export interface ContactFormProps {
  /** Optional class for the root element. */
  className?: string
}
